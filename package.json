{
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "start": "npm run watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "typescript": "^4.7.4"
  }
}